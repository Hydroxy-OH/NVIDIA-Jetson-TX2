Step3. Install PyTorch for python3 (refer to https://gist.github.com/dusty-nv/ef2b372301c00c0a9d3203e42fd83426)

# Add 8G swap
$ cd /mnt
$ sudo fallocate -l 8G swapfile
$ sudo chmod 600 swapfile
$ sudo mkswap swapfile
$ sudo swapon swapfile

# Clone pyTorch repository
cd ~/src
git clone http://github.com/pytorch/pytorch
cd pytorch
git submodule update --init

# Install pre-requirement
$ sudo pip3 install -U setuptools
$ sudo pip3 install -r requirements.txt
$ sudo pip3 install scikit-build
$ sudo pip3 install ninja

# Modify CMakeList.txt (line #100) as "opion(USE_NCCL "Use NCCL" OFF)" (refer to https://devtalk.nvidia.com/default/topic/1042821/pytorch-install-with-python3-broken/)
$ vim CMakeList.txt

# Install PyTorch
$ sudo python3 setup.py build
$ sudo python3 setup.py install
